Still in your blog3 folder:

"qcc -Vgcc_ntox86_64 hello.c -o hello"

Explanation:
qcc → QNX C compiler
-Vgcc_ntox86_64 → Target 64-bit QNX (x86_64)
hello.c → Source file
-o hello → Output executable




Check that the file was created:

"ls"

You should see:

hello.c  hello
